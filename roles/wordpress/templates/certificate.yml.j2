---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{deploy_name}}-tls
  namespace: {{namespace}}
spec:
  dnsNames: {{record_list}}
  secretName: {{deploy_name}}-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer